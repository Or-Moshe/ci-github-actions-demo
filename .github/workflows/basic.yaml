name: basic

on: 
  push:
    branches:
      - main
      
  workflow_dispatch:

jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: print message
        run: |
          echo "Hello, CI with GitHub Actions!."
  
